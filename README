# EntsCpp
C++ implementation of Ents Hierarchy Database.

See openpatterns.net/#ents for intended functionality.

Basic functionality of CLI explorer/editor has been added.

Use commands "f" to display the current Ent of focus, "c" to display its children, and "exit" to exit.
